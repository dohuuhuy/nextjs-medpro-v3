import React from 'react'
import styles from './styles.module.less'
import { PropsHeader } from '../../interface.header'

export const NavBottom = ({ dataHeader, Authencartion }: PropsHeader) => {
  const { menuMobile } = dataHeader
  return (
    <div>

    </div>
  )
}
